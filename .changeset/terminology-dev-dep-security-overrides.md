---
"@cosyte/terminology": patch
---

Security (dev-dependency advisory remediation; no runtime impact — `@cosyte/terminology` ships zero runtime dependencies, so the published artifact is unchanged). Added the shared canonical scoped `pnpm.overrides` block pinning two transitive **dev/build-time** packages to their patched releases: `esbuild` (`>=0.27.3 <0.28.1` → `0.28.1`; GHSA dev-server path-traversal — not reachable here: the library builds via `tsup`/`vitest` and never runs `esbuild serve`) and the `@changesets/parse` copy of `js-yaml` (`>=4.0.0 <4.2.0` → `4.2.0`; GHSA-h67p-54hq-rp68 quadratic merge-key DoS). The residual `js-yaml@3.x` reached through `read-yaml-file` (`@manypkg/get-packages` → `@changesets/cli`) is left in place deliberately: it calls `yaml.safeLoad`, removed/throwing in js-yaml 4, so it cannot be force-upgraded without breaking the release tooling, and it only parses trusted local repository YAML at release time. This mirrors `@cosyte/hl7` and the shared override block enforced suite-wide by the `@cosyte/config` drift check (SCAFFOLD-OVERRIDES-DRIFT backfill). The verify gate (format:check, build, typecheck, attw) is green on the upgraded dependency tree.
